Notes on making new images for KnowledgeFlow tutorial pdf:

png direct to eps:

convert file.png -compress lzw eps2:file.eps

// sometimes it seems that svn corrupts eps files (thinks they are entirely text even though they could contain binary image data):

svn add --no-auto-props file.eps

// convert PNGs to eps by:
// pngtopnm file.png > file.pnm
// pnmtops file.pnm > file.ps
// epstoeps file.ps file.eps


convert coat_of_arms.eps coat_of_arms.png
